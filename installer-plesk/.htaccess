<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all to install.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ install.php [L]
</IfModule>

DirectoryIndex install.php index.html index.htm
Options -Indexes
